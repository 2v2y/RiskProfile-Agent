# 发给学生3：阶段9前必须处理/确认的事项（更新版）

更新日期：2026-08-24
说明：本版已按验收复核更新。复核方式：对交付物做全量字段、哈希、行数检查，并逐项排除“字段名不同但内容相同”的情况；以下各项均确认为真实存在的缺口或待确认项。

## 一、必须由你处理/确认

### 1.【P0】案例输入卡补 Agent 必需字段（阶段9目前最大的阻塞）

- 现状：`benchmark_cases.jsonl`（5337 条）与 `red_team_cases.jsonl`（240 条）的 `input_card.allowed_profile_facts` 只有 21 个基础画像字段。对全部记录做递归键名检查，确认不存在任何同义字段或隐藏位置：`historical_standard_codes`、`historical_risk_categories`、`risk_category_counts`、`risk_category_unmapped_rate`、`risk_score`、`risk_percentile`、`model_version`、`score_evidence` 这 8 项**确实缺失**（gold 中也没有）。
- 来源：案例是从阶段4画像卡生成的，那张卡没有这几列。这 8 个字段的值，学生1 已在确认版 `profile_supplement_8fields.csv` 中按 `(sample_id, quarter)` 给全，与你的案例可 100% 匹配（bench 5337/5337、red_team 240/240）。
- 用在哪：Retrieval Agent 用历史标准编号+风险类别构造法规检索；Review/Audit 用风险分数与溯源字段。缺了这 8 项，跑案例时检索全部空转、全部转人工，B2/B5 对比失效。
- 需要你做什么（二选一）：
  - 方案A：重新生成案例集，把 8 个字段补进 `allowed_profile_facts`（字段值可以机械合并，由学生4代做合并脚本，**定稿由你确认**）；补齐后 `case_id/sample_id/quarter` 与 gold 保持 100% 对齐，`no_future_fields` 保持 true；
  - 方案B：书面确认正式实验改为直接读“画像+补充表”（不经过案例 input_card），并在 manifest 中写明。

### 2.【P1】版本号统一

- 现状：你的 gold 全部 5337 条 `knowledge_version=4.0`，你的 cases/red_team 也是 4.0；学生2 已在 08-24 把 cases/red_team 改成 `5.0-frozen`，新 manifest 也是 `5.0-frozen`。
- 用在哪：论文中知识库版本与 gold 的版本对齐声明。
- 需要你做什么：与学生2确认唯一版本号后，gold/cases/red_team/manifest 四个文件统一（机械改可由学生4代做并重算 SHA）。

### 3.【P1】金标访问控制

- 现状：`benchmark_gold_restricted.jsonl` 与案例同在 `origin_data/from_student3/` 普通目录；`.gitignore` 的受限规则只覆盖 stage1/stage9_edit 的 `data/00_raw_restricted/`，origin_data 没有任何限制。文件名带 restricted 不等于目录受限。
- 用在哪：gold 相当于考试答案，与题目同目录存在被调参过程误读的风险。
- 需要你做什么：把 gold 放入受限目录（或给出访问控制方案并声明）；文件移动可由学生4在集成时执行。

### 4.【P1】`expected_safe_defer_or_pass` 规则确认（已复核，无需补数）

- 现状：全量统计为 **4175 条 true / 1162 条 None**，与 `stratification.evidence_sufficiency` 完全对应（insufficient=4175 → true；sufficient=1162 → None）。语义已明确：证据不足的案例期望“安全转人工或在不编造前提下通过”，证据充分的案例无强制转人工期望。
- 用在哪：安全转人工率（safe_deferral_rate）指标。
- 需要你做什么：在 manifest 中写明该规则并一句话确认即可，不需要补逐条数值。

### 5.【P2】manifest 补文件 SHA-256

- 现状：`benchmark_manifest.json` 的 `output_files` 只给了路径，没有 `benchmark_cases.jsonl` / `benchmark_gold_restricted.jsonl` / `red_team_cases.jsonl` 的 SHA-256。
- 用在哪：复现实验时核对案例版本。
- 需要你做什么：补三个文件的 SHA-256（哈希可由学生4代算，你来确认/发布）。

### 6.【P2】`human_blind_sample.csv` 交付时间

- 现状：manifest 声称 100 条分层盲评样本，全盘搜索确认文件未交付（阻塞阶段10，暂不阻塞阶段9）。
- 需要你做什么：给出交付时间。

### 7.【P2】gold 口径正式表述

- 现状：gold 法规引用为“行业组参考代表标准”，非实体真实违反标准；R5/R6/R7/R9 代表标准在知识库无片段（`evidence_available=false`，共 21404 条引用）。
- 用在哪：引用准确率指标口径与论文限制声明。
- 需要你做什么：确认该口径在指标/论文中的正式表述，并确认评价程序对 `evidence_available=false` 单独统计的处理方式。

### 8.【P2】profile_version 统一

- 现状：cases/red_team/gold 全部为 `FREEZE_20260814_001`；程序加载画像时写的是 `student1-profile-v1`；补充表没有 profile_version 列。
- 需要你做什么：确认以 `FREEZE_20260814_001` 为准（loader 修改由学生4完成）。

## 二、已解决/已交付（供知悉）

- `benchmark_gold_restricted.jsonl` 与 `benchmark_manifest.json` 已补交（08-24 14:01）。
- 案例、gold、red_team 的 `case_id` 与画像键 100% 对齐；gold 引用的 chunk_id 全部存在于知识库（0 缺失）。
