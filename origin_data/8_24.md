# 8月24日工作汇报

汇报人：学生4


### 1. 完成阶段8的四个智能模块和固定流程

- 实现画像整理、法规检索、复核建议、内容审查四个模块；
- 增加独立语义审查，和程序核对器分开；
- 用 LangGraph 搭好固定流程：画像整理 → 法规检索 → 复核建议 → 内容审查 → PASS / 转人工 / 拒绝；
- 建立 `agent_registry`、`prompt_registry`、`forbidden_claim_rules` 三类登记文件；
- 建好正常、转人工、拒绝三类端到端样例；
- 阶段8验收测试 69/69 通过，阶段1回归测试 29/29 通过。

### 2. 在服务器上跑通 Qwen 和阶段1、阶段8

- 在服务器部署 vLLM Qwen3.8-27B；
- 跑通阶段1最小验证，确认 Qwen 不再输出固定模板；
- 跑通阶段8 LangGraph 固定流程，得到 PASS、转人工、拒绝三种结果；
- 排查并解决 Qwen 调用失败问题，主要是 `.env` 没加载、模型名不一致、Review 输出格式解析问题。

### 3. 检查学生1/2/3的新交付

- 读取 `origin_data` 里学生1、学生2、学生3新交的数据；
- 分别整理出缺失交付和需要确认的内容，写了三份待确认文档：
  - `待确认_学生1.md`
  - `待确认_学生2.md`
  - `待确认_学生3.md`
- 主要发现：
  - 学生1补了画像风险字段和 R1—R9 映射；
  - 学生2补了 FAISS 向量库和 `rag_retriever.py`；
  - 学生3补了 `benchmark_cases.jsonl` 和 `red_team_cases.jsonl`；
  - 但还缺 `benchmark_gold_restricted.jsonl`、`benchmark_manifest.json` 等。

### 4. 搭建阶段9实验骨架

- 新建 `operate/stage9_edit`，按研究方案推荐的目录结构组织；
- 把学生1/2/3需要的文件整理进 `data`、`knowledge`、`benchmark`；
- 实现 B0—B5 六个基线的统一运行框架；
- 实现学生2 FAISS/BGE 检索器的接入，并保留关键词回退；
- 写了指标计算和消融实验骨架；
- 离线干跑已通过，能正常生成 `case_*.json`、运行日志和清单。

### 5. 整理多份运行和交接文档

- `会议汇报_阶段8_20260822.md`
- `服务器运行与同步指南.md`
- `check_result.md`
- `待确认_学生1/2/3.md`

## 二、这些工作对项目的帮助

1. 阶段8是阶段9到12的地基。没有它，B0—B5 对比、消融实验、正式测试都没有统一的运行接口和日志。

2. 服务器 Qwen 跑通，证明系统不是只在本地假数据上能跑，而是能在真实模型环境里完成“画像 → 检索 → 生成 → 审查”的完整链路。

3. 检查学生交付物，把缺什么、哪里冲突提前暴露出来，避免后面实验做到一半才发现数据对不上。

4. 阶段9骨架让六个方法用同一批输入、同一套日志和输出，为公平比较和无依据陈述率、引用准确率、安全转人工率等指标做好准备。

5. 各类文档让服务器操作、结果查看、团队交接更清楚，减少反复沟通成本。

## 三、现在遇到的困难

### 1. 学生交付还不完整

- 学生3还缺 `benchmark_gold_restricted.jsonl`，导致阶段9只能生成输出，不能算正式指标；
- 学生3还缺 `benchmark_manifest.json` 和 `human_blind_sample.csv`；
- 学生2还缺 `retrieval_validation_metrics.csv`。

### 2. 几项数据口径需要确认

- 学生1的 R1—R9 映射和学生2的 `风险分类.csv`、研究方案里的定义不完全一致；
- 知识库版本出现 `4.0` 和 `5.0-frozen` 两种说法；
- `jurisdiction_context` 和旧模型 `context_site_state` 是否等价还没确认；
- 学生1的 `risk_percentile` 交付值是 0—100，程序里先按 0—1 做了转换，需要正式确认。

### 3. 服务器环境还需要最后确认

- 阶段9的 FAISS、sentence-transformers、torch 等依赖需要在服务器安装；
- Qwen 模型名需要和 vLLM 服务名完全一致；
- 本地代码修改需要同步到服务器后再做一次真实 Qwen 验证。

### 4. 正式指标暂时无法计算

因为缺少 gold 答案，阶段9目前只能做“六种方法能否跑通、能否落盘”的干跑，还不能输出无依据陈述率、引用准确率、安全转人工率等论文主指标。

## 四、下一步

1. 把本地改动同步到服务器，验证阶段8真实 Qwen 和阶段9干跑；
2. 继续推进学生3补 `benchmark_gold_restricted.jsonl` 和 `benchmark_manifest.json`；
3. 和学生1/2/3确认 R1—R9、知识库版本、`risk_percentile` 等口径；
4. 等 gold 补齐后，正式计算阶段9的六方法对比指标。
