# 学生4确认文档：阶段9前已确认项

确认日期：2026-08-24
确认人：学生1（刘知桦）
验证脚本：final_verify.py（已在WSL中运行，9项全部PASS）

---

## 最终验证结果：9 PASS, 0 FAIL

| # | 检查项 | 状态 | 详情 |
|---|--------|------|------|
| 1 | M2分数复算 | PASS | 5337行, 精确匹配100.0%, 最大差异5.00e-07 |
| 2 | R1-R9三文件一致 | PASS | 9个类别全部匹配 |
| 3 | model_hash为64位 | PASS | 长度=64, 值=562d7837e0d2bc63... |
| 4 | model_hash与manifest一致 | PASS | manifest与supplement均为562d7837... |
| 5 | model_hash与实际文件一致 | PASS | frozen_model.joblib SHA-256匹配 |
| 6 | score_evidence包含完整hash | PASS | 64位SHA-256 |
| 7 | 字段映射完整 | PASS | 2个映射: candidate_naics_group→industry_group, cutoff_date→ranking_cutoff |
| 8 | Pipeline含ColumnTransformer | PASS | 可自动处理字符串列 |
| 9 | Pipeline含LogisticRegression | PASS | 分类模型 |

---

## 第1项：R1—R9 分类映射一致性

### 结论：无冲突，三份文件完全一致

对方文档中提到的 `agent_profile_whitelist.json` 使用 "R1_坠落防护、R2_电气" 与 `standard_to_r1r9_mapping.csv` 冲突的问题，**经核查当前文件不存在**。对方可能看到了旧版本。

### 三文件 R1—R9 名称对比

| 类别 | standard_to_r1r9_mapping.csv | agent_profile_whitelist.json (KEY) | 风险分类_R6修正.csv |
|------|------------------------------|-------------------------------------|---------------------|
| R1 | 电气危险与带电防护 | R1_电气危险与带电防护 | R1 |
| R2 | 个人防护装备 | R2_个人防护装备 | R2 |
| R3 | 能量隔离与上锁挂牌 | R3_能量隔离与上锁挂牌 | R3 |
| R4 | 高处作业与坠落防护 | R4_高处作业与坠落防护 | R4 |
| R5 | 作业许可、培训与程序执行 | R5_作业许可、培训与程序执行 | R5 |
| R6 | 机械设备与工器具安全 | R6_机械设备与工器具安全 | R6 |
| R7 | 受限空间、消防与危险环境 | R7_受限空间、消防与危险环境 | R7 |
| R8 | 现场通道、物体打击与综合防护 | R8_现场通道、物体打击与综合防护 | R8 |
| R9 | 其他或尚未分类 | R9_其他或尚未分类 | R9 |

### SHA-256

| 文件 | SHA-256 |
|------|---------|
| standard_to_r1r9_mapping.csv | 76C0311FE83097F8F72B4F1688DB6503199659C9F2B9B54C52721047DA444D66 |
| agent_profile_whitelist.json | 5F2FA6A4F44B70D3693B28C9DCFD3B7795838483BC5AB4019055DBCB55CA4F1A |
| profile_supplement_8fields.csv | B254994ABC5D260A5ACF27FCF873CFC6552972D92F6A966608C5D294ACAD2835 |

---

## 第2项：画像字段与 Agent Schema 的映射

### 结论：映射已在 agent_profile_whitelist.json 中定义

`agent_profile_whitelist.json` 的 `field_name_mapping` 段已写明：

| profiles_train_val.csv 列名 | Agent Schema 字段名 |
|------------------------------|---------------------|
| candidate_naics_group | industry_group |
| cutoff_date | ranking_cutoff |

其他字段直接对应，无需映射：
- `historical_risk_categories` → Schema 同名字段
- `risk_category_counts` → Schema 同名字段
- `risk_categories`：中文描述列（如 "R1_电气危险与带电防护:1;R8_现场通道:2"），不是程序输入字段

---

## 第3项：jurisdiction_context 与 context_site_state 等价性

### 结论：同一字段，冻结后改名，可直接互换

`jurisdiction_context` 和 `context_site_state` 是同一个数据：截点前最近一次成熟历史检查的 site_state。

### 时间线

| 时间 | 事件 | config.yaml | CSV列名 | frozen_config.json |
|------|------|-------------|---------|---------------------|
| 2026-08-17 | 模型冻结 | context_site_state | context_site_state | context_site_state |
| 2026-08-20 | 改名+移除PII | jurisdiction_context | jurisdiction_context | 未修改 |

frozen_config.json 未修改（SHA-256: 6540448b98e9fb6ebc1ed49124cee1d4ade217e950c32cba470241410fffbf87）。

### 模型实际结构

frozen_model.joblib 内部使用 sklearn Pipeline：
- Step 0: ColumnTransformer（OneHotEncoder 处理 category 列 + StandardScaler 处理 numeric 列 + MissingIndicator）
- Step 1: LogisticRegression

Pipeline 的 ColumnTransformer 会自动处理字符串列（OneHotEncoder），不依赖 `to_numpy(dtype=float)`。复算时只需将 `jurisdiction_context` 重命名为 `context_site_state` 即可。

---

## 第4项：risk_score 复算验证

### 结论：验证通过，100% 精确匹配

**验证方法**：在 WSL 中加载 frozen_model.joblib（Pipeline），将 `jurisdiction_context` 重命名为 `context_site_state` 后运行 `predict_proba`，与 `profile_supplement_8fields.csv` 中的 risk_score 逐行比对。

**验证结果**：

| 指标 | 值 |
|------|-----|
| 总比对行数 | 5337 |
| 精确匹配 (diff < 1e-6) | 5337 (100.0%) |
| 近似匹配 (diff < 1e-4) | 5337 (100.0%) |
| 最大差异 | 5.00e-07（浮点精度级别） |
| 状态 | **PASS** |

### model_hash 与 score_evidence

- `score_evidence`：完整 64 位 SHA-256 = `562d7837e0d2bc6389e95607e543f9b07db5be1b1fd0ad05487a1151b5001603`
- `model_hash`：已修复为完整 64 位（之前是截断的 16 位，现已更正）
- 该 SHA-256 与 frozen_manifest.json 中 `files.frozen_model.sha256` 一致
- 该 SHA-256 与 frozen_model.joblib 实际文件 SHA-256 一致

### 已修复项

`profile_supplement_8fields.csv` 中的 `model_hash` 字段已从截断值（16位）更正为完整 SHA-256（64位）。

---

## 文件清单

本包包含以下文件：

| 文件 | 说明 |
|------|------|
| 学生4_确认文档_20260824.md | 本文档，4项确认结论+最终验证结果 |
| agent_profile_whitelist.json | 白名单（R1-R9一致，字段映射定义） |
| profile_supplement_8fields.csv | 8字段补充（model_hash已修复为64位） |
| standard_to_r1r9_mapping.csv | R1-R9标准映射 |
| final_verify.py | 综合验证脚本（可在WSL重跑） |
| risk_score_verification.csv | 5337行逐行对比结果 |
| final_verification_report.txt | 最终验证报告（9 PASS, 0 FAIL） |

### 重跑验证

```bash
cd /home/liu/osha && .venv/bin/python3 /mnt/c/Users/LENOVO/Desktop/学生4_确认包_20260824/final_verify.py
```

---

## 总结

| 项 | 状态 | 是否阻塞阶段9 |
|----|------|--------------|
| 1. R1-R9映射 | 无冲突，三文件一致 | 不阻塞 |
| 2. 字段映射 | 已在whitelist定义 | 不阻塞 |
| 3. 字段等价性 | 同一字段，冻结后改名 | 不阻塞 |
| 4. risk_score验证 | 100%匹配，PASS | 不阻塞 |

**最终验证：9 PASS, 0 FAIL，全部通过。**
