# 发给学生1：阶段9前必须处理/确认的事项（更新版）

更新日期：2026-08-24
说明：本版已按验收复核更新。复核方式：对交付物做全量字段、哈希、行数检查，并逐项排除“字段名不同但内容相同”的情况；以下各项均确认为真实存在的缺口或待确认项。

## 一、必须由你处理/确认

### 1.【P1】`model_hash` 全量修复

- 现状：确认包 `profile_supplement_8fields.csv` 共 5337 行，其中 3345 行已是完整 64 位哈希，**1992 行（train 1757 + validation 235）仍为 16 位截断值** `562d7837e0d2bc63`（该值 = 完整哈希前 16 位）。`score_evidence` 5337 行全部含完整 64 位哈希，与模型文件一致。`final_verify.py` 只检查第一行（`iloc[0]`），所以显示“9 PASS”，实际未覆盖全量。
- 为什么必须你：补充表是你的交付物；需要你确认这 1992 行都应等于模型文件哈希 `562d7837e0d2bc6389e95607e543f9b07db5be1b1fd0ad05487a1151b5001603`（或由你重跑生成）。
- 用在哪：Audit Agent 溯源核对；论文可复现性；validation 批次（阶段9常用）有 235 行受影响。
- 需要你做什么：全量修复并重跑覆盖全部 5337 行的验证脚本，回复新文件 SHA-256。

### 2.【P1】`risk_percentile` 口径书面确认

- 现状：交付值为 0.4—100（百分位口径），Schema 与白名单定义 0—1；08-24 确认文档中没有任何关于该字段的说明（该项确认实际尚未完成）。
- 为什么必须你：字段语义只能由你确认。
- 用在哪：Review Agent 输出“风险分位前 X%”，Audit Agent 做数字核对。
- 需要你做什么：书面确认“交付口径 = 0—100 百分位，程序按 /100 转为 0—1”即可。

### 3.【P1】R1—R9 映射生成源

- 现状：`standard_to_r1r9_mapping.csv`（6346 行）含 **R6=179 行**，其中 1910.0179、1910.0181、1910.0184、1926.14 等标准**不在**交付的 `风险分类.csv` 复核主类（R6=23 个标准）里，最终主类 R6=0；映射声称“源自风险分类.csv AD列”，但无法从交付表复现。交付物及所有 zip 中均没有 `风险分类_初分.csv`、`风险分类_复核.csv`、`风险分类_R6修正.csv` 原件。
- 为什么必须你：没有生成脚本或原件，无法重建 R6 这 179 行。
- 用在哪：画像 `risk_category_counts`/`historical_risk_categories` → Retrieval Agent 按风险类别查法规 → gold 的 R1—R9 覆盖声明。
- 需要你做什么：提供映射生成脚本，或 `风险分类_R6修正.csv` 原件；确认最终冻结映射的生成源与 SHA-256。

### 4.【P2】白名单唯一版本确认

- 现状：共 4 个版本并存——学生3 08-20 旧版（运行入口仍在使用）、你 08-21 v1.0（R1—R9 为旧名称）、08-24 确认包 v1.1、stage9 里的 confirmed 副本（未接线）。
- 用在哪：决定 Agent 能读取哪些画像字段，直接影响 Profile/Review/Audit 与论文声明。
- 需要你做什么：确认 v1.1 是唯一最终版，并说明旧版处置方式（配置切换由学生4完成）。

## 二、已解决/已交付（供知悉）

- R1—R9 类别名称在三份文件（映射/确认包白名单/R6修正参照）之间已确认一致；但上述第 3 项的映射生成源仍需补齐。
- `risk_score` 复算验证已交付（5337 行，最大差异 ≤5e-07）；模型文件哈希与 `frozen_manifest.json`、`score_evidence` 一致。
