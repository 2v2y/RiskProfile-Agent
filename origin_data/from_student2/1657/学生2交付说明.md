# 学生2交付说明（供学生4及AI助手使用）

交付日期：2026-08-26
知识库版本：6.0-frozen
交付人：学生2

---

## 一、交付文件清单（13项）

| 序号 | 文件 | 位置 | 用途 |
|------|------|------|------|
| 1 | `rag_retriever.py` | `src/retrieval/` | 检索接口，学生4直接调用 |
| 2 | `faiss_index.bin` | `knowledge/vector_db/` | FAISS向量索引 |
| 3 | `embeddings.npy` | `knowledge/vector_db/` | 22781条片段的BGE向量 |
| 4 | `chunk_ids.json` | `knowledge/vector_db/` | FAISS索引到chunk_id的映射 |
| 5 | `db_meta.json` | `knowledge/vector_db/` | 向量库元信息 |
| 6 | `regulation_chunks.jsonl` | `knowledge/chunks/` | 22781条法规片段正文 |
| 7 | `document_inventory.csv` | `knowledge/` | 366条法规文档台账 |
| 8 | `standard_document_mapping.csv` | `knowledge/` | 标准编号↔片段编号对应表 |
| 9 | `retrieval_gold.csv` | `knowledge/` | 93条验证集查询+正确答案 |
| 10 | `retrieval_validation_metrics.csv` | `knowledge/` | 检索验证结果（3种方法×4个k值） |
| 11 | `knowledge_manifest.json` | `knowledge/` | 知识库版本与配置 |
| 12 | `standard_to_r1r9_mapping.csv` | `knowledge/` | R1-R9风险分类映射（学生1制作，最终版） |
| 13 | `学生4检索程序接入指南.md` | 根目录 | 详细接入说明 |

---

## 二、检索接口调用方式

```python
from rag_retriever import RAGRetriever

retriever = RAGRetriever()
results = retriever.search("1910.132", k=3)

# 返回格式：list[dict]
# 每条结果含：chunk_id, standard, section, text, score
```

- 标准编号查询（如"1910.132"）：精确过滤 + section匹配优先 + BGE语义排序
- 自然语言查询：纯BGE语义检索
- 推荐参数：`k=3`

---

## 三、检索验证结果

验证集：93条查询（标准编号类型）
评估指标：Recall@k, Precision@k, MRR, NDCG@10

| 方法 | k | Recall | MRR | NDCG@10 |
|------|---|--------|-----|---------|
| 混合检索（推荐） | 3 | **1.0000** | **0.8530** | **0.8914** |
| 混合检索 | 10 | 1.0000 | 0.8530 | 0.8914 |
| 纯BGE向量 | 10 | 0.0000 | 0.0000 | 0.0000 |
| 关键词(TF-IDF) | 10 | 0.6559 | 0.2290 | 0.3296 |

结论：混合检索（精确过滤+section匹配+BGE排序）效果最优，Recall@3=100%。

---

## 四、R1-R9风险分类映射

使用 `knowledge/standard_to_r1r9_mapping.csv`（学生1制作，最终版）。

**注意编号格式差异**：
- 映射表用的是DOL格式：`19100269`、`19261050`
- 知识库chunks用的是标准格式：`1910.269`、`1926.1050`
- 转换方法：去掉前4位前缀（1910/1926），剩余部分去前导零，加回点号
  - `19100269` → 去前缀`1910` → `0269` → 去前导零 → `269` → `1910.269`
  - `19261050` → 去前缀`1926` → `1050` → 无前导零 → `1926.1050`

映射表列名：`standard_code, standard_normalized, r_category, r_category_name, basis`

R分类列表：R1-R9（共9类）

| R分类 | 名称 |
|-------|------|
| R1 | 电气危险与带电防护 |
| R2 | 个人防护装备 |
| R3 | 能量隔离与上锁挂牌 |
| R4 | 高处作业与坠落防护 |
| R5 | 作业许可、培训与程序执行 |
| R6 | 机械设备与工器具安全 |
| R7 | 受限空间、消防与危险环境 |
| R8 | 现场通道、物体打击与综合防护 |
| R9 | 其他或尚未分类 |

---

## 五、版本一致性

所有文件版本号统一为 **6.0-frozen**：

| 文件 | 版本字段 | 值 |
|------|---------|-----|
| `knowledge_manifest.json` | 知识库版本 | 6.0-frozen |
| `db_meta.json` | db_version | 6.0-frozen |
| 学生3的 `benchmark_cases.jsonl` | knowledge_version | 6.0-frozen（需学生3更新） |

---

## 六、环境依赖

```
python >= 3.10
faiss-cpu
sentence-transformers
torch
numpy
```

BGE模型：`BAAI/bge-small-en-v1.5`（384维）
- 首次运行时从HuggingFace自动下载
- 若服务器离线，需提前缓存模型到本地，设置环境变量：
  ```
  export TRANSFORMERS_CACHE=/path/to/model_cache
  ```
- 向量库文件（faiss_index.bin, embeddings.npy, chunk_ids.json, db_meta.json）为同一次生成，互相匹配，请勿混用

---

## 七、向量库与片段的对应关系

```
chunk_ids.json       → chunk_id列表，顺序对应FAISS索引
faiss_index.bin      → FAISS索引（22781个向量，384维）
embeddings.npy       → 原始向量矩阵（22781×384）
regulation_chunks.jsonl → 22781条片段正文
```

通过 `chunk_ids.json` 的索引位置可以找到对应的 `chunk_id`，再用 `chunk_id` 在 `regulation_chunks.jsonl` 中查找完整内容。`rag_retriever.py` 已封装了这整套逻辑。

---

## 八、已处理的修复记录

| 修复项 | 说明 |
|--------|------|
| 补齐法规标准 | 从93个标准（4656片段）扩充到273个标准（22781片段），覆盖29 CFR Part 1910/1926全部现行标准 |
| 重建FAISS向量库 | 4656向量→22781向量，版本号 5.0-frozen→6.0-frozen |
| 修复元数据空值 | standard_document_mapping.csv 修复全部13766条空值 |
| 补全文档台账 | document_inventory.csv 补全SHA-256/片段数/字符数字段，文档数193→366 |
| 剔除无效编号 | 1926.1501、1926.202（eCFR与OSHA官网均无此标准） |
| 统一来源渠道 | 366条文档保证至少有一个官方渠道（osha.gov或ecfr.gov） |
| 修复检索策略 | 增加section精确匹配+float比较，Recall@3从0.26提升至1.00 |
| 修正NDCG@10 | 原为N/A，已全部算出真实值 |
| 修复解释资料片段编号 | 从 `interpretation_YYYY-MM-DD` 改为 `OSHA-INTERP-YYYY-MM-DD` |
| 补充R1-R9映射表 | 采用学生1最终版，补齐29条缺失标准；1926 Subpart CC起重机同族统一归R6（消除1400=R6与1401+=R8的分裂），266个联邦标准全覆盖 |

---

## 九、待学生4确认事项

1. **离线环境**：确认服务器已安装 faiss-cpu、sentence-transformers、torch，BGE模型已缓存
2. **benchmark版本**：学生3需将 `benchmark_cases.jsonl` 的 knowledge_version 改为 6.0-frozen，学生4确认使用该版本
3. **R1-R9映射编号转换**：映射表用DOL格式，知识库用标准格式，学生4的代码需做格式转换（转换方法见第四节）