# 发给学生2：阶段9交付确认（更新版 2026-08-24 晚）

说明：`from_student2/交付_学生4`（08-24 晚更新版）已完成第二轮验收复核。**核心知识库与检索交付已达标，可以使用**。以下为“已解决”记录与仍需处理/注意的事项。

## 一、已解决（复核通过）

1. **版本统一 5.0-frozen**：knowledge_manifest.json、db_meta.json、接入指南一致；与学生3 的 gold/cases 版本对齐。
2. **retrieval_validation_metrics 补 nDCG**：12 行全部有真实值（hybrid k=3：Recall=1.0000、MRR=0.8530、NDCG@10=0.8914）。
3. **解释资料/非法规正文编号修复**：chunks 与 mapping 的 13 条编号改为 OSHA-INTERP-*（chunk_id 不变）；gold 29906 条 chunk 引用 0 缺失；检索 gold 0 缺失；向量库索引顺序与 chunks 一致。
4. **检索策略修复**：section 精确匹配 + float 比较，Recall@3=100%；代码/指标/说明三者一致。
5. **交付说明**：文件清单、修复记录、环境依赖齐全。

## 二、仍需你处理/说明

### 1.【P1】包内 `standard_to_r1r9_mapping.csv` 是旧版，请替换或更正

- 现状：包内映射（SHA `fe623738…`）无 R6（R6=0、R8=777）；学生1 08-24 最终版（SHA `76c0311f…`）有 R6=179、R8=598。你的 `学生2交付说明.md` 中 R 分类表（“R1-R9（无R6）”，R3=材料储存与搬运、R5=工具与手工作业）也与官方 `04_风险分类规则.md` 和学生1最终版不一致。
- 影响：若误用包内旧映射，机械类（R6）会归入 R8，画像分类、gold 覆盖、按类别检索全部错位。
- 处理：请把包内映射替换为学生1最终版（SHA `76c0311f…`）并更正说明文字；或明确标注包内旧版作废、以学生1版本为准。

### 2.【P2】向量库与 08-24 晚 chunks 的一致性说明

- faiss_index.bin / embeddings.npy 仍为 08-21 生成；chunks 仅 13 条 standard 标签变化、text 未变。请说明嵌入输入是否包含 standard 字段，或确认无影响。

### 3.【P2】旧 zip 与新目录不一致

- `student2_1609.zip`（16:00）与 `交付_学生4.zip`（11:15）为旧版内容；当前目录（18:45）为最新。建议重打 zip 或标注旧包过期，避免误用。

### 4.【P2】检索验证数字与上一版差异大，建议附评估脚本

- 08-24 11:15 版 MRR=1.0、纯BGE Recall@3=0.5054、keyword 22 条；08-24 晚版 MRR=0.8530、纯BGE Recall=0、keyword 93 条。数字变化可以解释，但建议提供评估脚本/说明以便复现。

## 三、学生4 集成侧待办（不是你的交付缺口）

- 同步新包进 stage9 运行目录并登记 SHA；R1-R9 映射以学生1最终版为准；stage9 检索适配器补 document_id 回退（57 条非法规正文片段）；服务器安装 faiss/sentence-transformers/torch 并缓存 BGE；确认使用 5.0-frozen 案例版本。


