# 发给学生2：阶段9前需要补充或确认的内容

记录日期：2026-08-23

说明：下面按是否阻塞阶段9排序。已交付的知识库五件套和 FAISS 向量库不用重复提交，只补缺项和确认结论。

## 1. `retrieval_validation_metrics.csv` 缺失（未交付，阻塞阶段9）

现状：

接入指南里写了 Recall@3=1.0、MRR=1.0，但正式交付物里没有 `retrieval_validation_metrics.csv` 这个文件。

对模型的影响：

没有这份正式记录，阶段6“检索方法和每次返回条数已经确定”就无法落在可复现文件上；阶段9写实验方法和论文方法部分时，没有文件可以证明这些参数是在验证集上选定的。

需要你做什么：

交付 `retrieval_validation_metrics.csv`，至少包含：

- `method`：keyword / vector / hybrid；
- `top_k`；
- `n_queries`；
- `recall_at_5`；
- `mrr`；
- `ndcg_at_10`；
- `eval_split`；
- `config_hash`；
- `created_at`。

## 2. 知识库版本需要统一（待确认，阻塞阶段9）

现状：

- 你的 `knowledge_manifest.json` 实际写的是 `知识库版本: "4.0"`；
- 但 `检索程序接入说明_学生4.md` 写的是 `5.0-frozen`；
- FAISS 向量库 `db_meta.json` 写的是 `db_version: "1.0"`；
- 学生3的 `benchmark_cases.jsonl` 写的是 `knowledge_version: "4.0"`。

对模型的影响：

如果评价案例指向的法规库版本和实际检索的法规库版本不一致，正确答案会错位，引用准确率、Recall、MRR 都不可信，B2/B5 的结果无法公平比较。

需要你做什么：

确认最终冻结的知识库版本号：如果以 `4.0` 为准，就更新接入说明里的 `5.0-frozen` 表述；如果以 `5.0-frozen` 为准，就更新 `knowledge_manifest.json`，并让学生3把 `benchmark_cases.jsonl` 和 `red_team_cases.jsonl` 里的 `knowledge_version` 改成一致版本。

## 3. R1—R9 映射需要和学生1的映射对齐（待确认，阻塞阶段9）

现状：

学生2有 `风险分类.csv`，学生1又交付了 `standard_to_r1r9_mapping.csv`，两者看起来不完全一致。

对模型的影响：

`knowledge/chunks/regulation_chunks.jsonl` 里的 `risk_categories` 字段如果和学生1的 R1—R9 映射不一致，按风险类别检索会漏检或错检，Review 引用和 Audit 类别检查都会错误。

需要你做什么：

- 确认 `风险分类.csv` 是否最终冻结版；
- 说明法规片段里的 `risk_categories` 是否就是按该表生成的；
- 和学生1、学生3确认 R1—R9 的最终统一版本。

## 4. `rag_retriever.py` 和 FAISS 向量库的离线可用性需要确认（待确认，阻塞接入）

现状：

接入指南要求 `faiss-cpu`、`sentence-transformers`、`torch`，并首次自动下载 `BAAI/bge-small-en-v1.5` 模型。

对模型的影响：

如果服务器不能访问 HuggingFace，或者缺少这些依赖，正式检索器加载不出来，B2/B5 会退回旧检索或直接失败，阶段9无法进行。

需要你做什么：

- 确认服务器上这些依赖和 BGE 模型是否已经可用；
- 如果模型在服务器本地已缓存，给出本地路径；
- 确认 `faiss_index.bin`、`chunk_ids.json`、`db_meta.json` 和 `regulation_chunks.jsonl` 是同一次冻结生成、互相匹配。
