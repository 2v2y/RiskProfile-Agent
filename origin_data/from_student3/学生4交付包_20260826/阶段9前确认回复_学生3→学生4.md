# 回复学生4：阶段9前待确认事项处理结论（学生3 → 学生4）

回复人：冯婧（学生3）　日期：2026-08-24　对应文件：`待确认_学生3.md`（更新版）
产物目录：`新版项目/新版本论文产物/学生4需要的文件/`

## 一、总览

| # | 优先级 | 事项 | 学生3 处理结论 | 状态 |
|---|---|---|---|---|
| 1 | P0 | 案例输入卡补 Agent 必需 8 字段 | **方案B 已锁定**；真·补充表已找到并落库，查表已生成 | ✅ 数据已就位；`case_8fields_lookup.jsonl` 已生成（5577 条，100% 命中） |
| 2 | P1 | 知识库版本号统一 | gold 4.0 → 5.0-frozen（2026-08-24）→ **6.0-frozen**（2026-08-26 学生2 第二份通知），四文件现已统一 6.0-frozen | ✅ 已完成 |
| 3 | P1 | 金标访问控制 | 给出受限目录 + 物理隔离方案，集成时由你执行移动 | ✅ 方案已给 |
| 4 | P1 | expected_safe_defer_or_pass 规则 | 4175 true / 1162 None 规则已写入 manifest | ✅ 已确认 |
| 5 | P2 | manifest 补文件 SHA-256 | 三个 jsonl 的 SHA-256 已补入 output_files | ✅ 已完成 |
| 6 | P2 | human_blind_sample.csv 交付 | v2.0（100 行×11 列）已交付，由学生5 填 evaluation 列 | ✅ 已交付（给的是学生5） |
| 7 | P2 | gold 口径正式表述 | "行业组参考代表标准"口径 + evidence_available=false 单独统计，已写入 manifest | ✅ 已确认 |
| 8 | P2 | profile_version 统一 | 以 FREEZE_20260814_001 为准，loader 由你改 | ✅ 已确认 |

> 一句话：**8 项全部由学生3 给出结论/产出。其中 Item 1（P0）已找到真·补充表并实跑关联脚本生成 `case_8fields_lookup.jsonl`（5577 条、100% 键命中），转为"数据已就位"；唯一残留为 `historical_standard_codes` 约 37% 空值（安全锁残留，归属学生1）。其余 7 项（P1/P2）已于前轮完成。**

---

## 二、逐项说明

### 1.【P0】案例输入卡补 Agent 必需 8 字段 —— 决议：方案B

- **背景冲突**：我方原立场是"M2 不考虑，阶段7 不引 risk_score/risk_percentile/risk_category_counts 等"（manifest m2_model）。你方指出这 8 项（historical_standard_codes、historical_risk_categories、risk_category_counts、risk_category_unmapped_rate、risk_score、risk_percentile、model_version、score_evidence）是 Retrieval/Review/Audit Agent 跑起来必需的，缺了"检索空转、B2/B5 对比失效"。
- **学生3 决议：方案B**（书面确认，不需要改案例 input_card）：
  - 正式实验由 Agent **直接读取** `profiles_train_val.csv`（画像主表）+ 一份**运行时生成的 8 字段查表**（按 `sample_id, quarter` 关联）来获取这 8 项；该查表非静态文件，须由学生2 完成 R1-R9 逐样本映射、学生4 完成 M2 预测后合并而成（见下方更正）；
  - 案例 `input_card.allowed_profile_facts` **不内嵌**这 8 项，从而保持 `no_future_fields=true` 与"M2 排除"立场一致，且案例文件不需重生成、case_id/sample_id/quarter 与 gold 天然对齐。
- **为何不选手方案A**：方案A 会把 M2 相关字段塞回案例卡，与 m2_model.used=false 及 no_future_fields=true 立场冲突；且经回查，学生1 侧并无现成 8 字段可内嵌。方案B 同时满足"Agent 能拿到 8 字段"和"案例卡保持纯净"两个目标。
- **更正（2026-08-24 回查学生1《00_新生成物规则与变动说明.md》）**：此前"学生1 已在 profile_supplement_8fields.csv 给全 8 项"不成立。该文件不存在，且 8 字段 student1 无法单独产出，分属：
  - `historical_standard_codes`：学生1 可提取，但 Test 开封后安全锁挡住（§8.2）；
  - `historical_risk_categories` / `risk_category_counts` / `risk_category_unmapped_rate`：**学生2** 的 R1-R9 分类表（standard_to_r1r9_mapping.csv，已交付 5.0-frozen）逐样本映射；
  - `risk_score` / `risk_percentile` / `model_version` / `score_evidence`：**学生4** 的 M2 模型最终版输出。
  → P0 是**跨学生生成任务**，不是"学生1 交表"。
- **本轮落地（manifest 已升 v2.3-20260824-routeA，SHA `bb5a5f07…`，两处一致）**：
  1. `p0_resolution.status` 改为 `decided_planB; BLOCKED_on_cross_team_generation`；新增 `true_source_breakdown`（8 字段逐一定义来源）；`join_spec` 右表改为"生成的 8 字段查表"；`fallback` 改为"若 M2 不可得，论文声明 model_output 4 字段不可得并调整 B2/B5 口径"。
  2. `p0_join_8fields_template.py` 的"单一学生1 补充表"假设需相应改为"学生2 映射 + 学生4 预测合并"（脚本待学生4 在集成时按实际来源改输入）。
- **残余待办（非学生3 可闭环，需跨学生）**：
  1. **学生2**：对每样本 historical_standard_codes 用 R1-R9 映射，产出 risk_composition 4 字段；
  2. **学生4**：运行 M2 模型最终版，产出 model_output 4 字段；
  3. **学生1**：解除安全锁或经独立脚本生成 `historical_standard_codes`；
  4. 学生4 在集成时将上述合并为按 (sample_id,quarter) 的 lookup，供 Agent 运行时读取。
- **本项不阻塞阶段9 启动**：案例卡已纯净（8 字段 0 命中、no_future_fields 100% true），阶段9 可先跑；仅"8 字段实际被 Agent 读取"待上述跨学生产出就位。若学生4 的 M2 最终版确不可得，按 fallback 在论文声明并调整 B2/B5 口径即可。

### 1b.【P0 二次更正-2026-08-24 17:58】真·补充表已找到，查表已生成

- **更正事实**：用户从 `刘知桦819\profile_supplement_8fields.csv` 找到真·补充表（14 列 × 5337 行，P0 的 8 字段**全部齐全**，关联键 `sample_id,quarter` 在位）。此前在 `刘知桦819\学生3_模型训练\` 子目录漏检，故上轮得出"BLOCKED_on_cross_team_generation / 8 字段跨学生生成"的判断**作废**。
- **已落库**：`学生4需要的文件/profile_supplement_8fields.csv`（来源已记入 manifest）。
- **已实跑关联脚本**：`p0_join_8fields_template.py` 生成 `学生4需要的文件/case_8fields_lookup.jsonl`：
  - 记录数 **5577**（benchmark 5337 + red_team 240）；关联键 100% 命中（bench 5337/5337、red 240/240）；
  - SHA `86165990…`；Agent 运行时按 `sample_id,quarter` 读此查表即可，**不回写案例 input_card**（保持纯净）。
- **字段覆盖**：`historical_risk_categories` / `risk_category_counts` / `risk_category_unmapped_rate` / `risk_score` / `risk_percentile` / `model_version` / `score_evidence` 共 7 项 **100% 有值**；仅 `historical_standard_codes` **约 37% 空值（1992/5337）**——与"安全锁挡住学生1 提取"一致，属学生1 侧残留，**不阻塞**阶段9，仅使该字段覆盖率约 63%。
- **manifest 已升 v2.4-20260824-routeA**（SHA `63a9e8d1…`，两处一致）：`p0_resolution.status` 改为 `resolved_data_available; lookup_generated`，并补 `data_source` / `lookup_generated` / `field_coverage` 三段结构化记录。
- **你（学生4）现在要做的**：集成时直接读 `case_8fields_lookup.jsonl`（按 sample_id,quarter 取 8 字段），无需再跑脚本。"historical_standard_codes 空值样本"请让 Retrieval Agent 降级处理（跳过法规检索或改用语义检索）；若学生1 后续补齐该字段，重跑 `p0_join_8fields_template.py` 即可刷新查表。

### 2.【P1】知识库版本号统一 —— 已完成

- 之前 cases/red_team/manifest 已为 `5.0-frozen`，仅 gold 仍为 `4.0`。
- 本轮将 `benchmark_gold_restricted.jsonl` 全部 `knowledge_version` 由 `4.0` 改为 `5.0-frozen`（每条记录含多个法规引用标签，全文件共 **56647 处**，0 残留），JSON 解析校验 5/5 通过。
- 新 SHA-256：`532e0af3e36200486012723c13060886f2c709739f67ee86626843144ccb5436`（见本文件夹 gold 副本与 manifest）。
- **更新（2026-08-26 学生2 第二份通知）**：知识库重新冻结为 **6.0-frozen**（补齐缺失标准：4656 片段→22781 片段、193 文档→366 文档）。已将 cases(5337处)/red_team(240处)/gold(56647处)/manifest 的 `knowledge_version` 全部由 `5.0-frozen` 改为 `6.0-frozen`（正则精确替换字段值，JSON 校验通过，LF 行尾）。**新 SHA**：cases `bd79861e…`、red_team `1521faf3…`、gold `bf72307a…`；manifest 升 v2.5-20260826-routeA（SHA `5aecfe77…`）。请以 **6.0-frozen** 为准对接检索库版本。

### 3.【P1】金标访问控制 —— 方案已给，集成时执行

- gold 相当于"考试答案"，不应与题目（cases/red_team）同普通目录。
- 方案：将 `benchmark_gold_restricted.jsonl` 置于受限目录（如 `data/00_raw_restricted/` 或 `origin_data/from_student3/` 下受限子目录），与案例输入**物理隔离**；正式集成前由你执行移动并声明访问控制。
- 本 workspace 当前位于 `阶段7/阶段7产物/`，**正式集成前应迁移**（已在 manifest Item3 注记）。

### 4.【P1】expected_safe_defer_or_pass 规则 —— 已确认

- 全量统计 **4175 条 true / 1162 条 None**，与 `stratification.evidence_sufficiency` 完全对应（insufficient=4175→true；sufficient=1162→None）。
- 语义：证据不足案例期望"安全转人工或在不编造前提下通过"；证据充分案例无强制转人工期望。无需补逐条数值，规则已写入 manifest。

### 5.【P2】manifest 补文件 SHA-256 —— 已完成

- `output_files` 已补三个 jsonl 的 SHA-256：
  - benchmark_cases.jsonl：`a21dece3f0777a20854a2ed0e47251cec3726bf4850c977080990f3573c46161`
  - benchmark_gold_restricted.jsonl：`532e0af3e36200486012723c13060886f2c709739f67ee86626843144ccb5436`
  - red_team_cases.jsonl：`d173b182b5a2fe54ba587df8ddb5013ed4b5ff7fa8e913999154c302d066c6b0`

### 6.【P2】human_blind_sample.csv 交付时间 —— 已交付

- 文件已生成：`阶段7/阶段7产物/human_blind_sample.csv`（v2.0，100 行 × 11 列，方法名 B0–B5 隐藏）。
- 评价方案 v2.0：从原 5 维百分制（8 列）简化为单列二元 `evaluation`（yes=易懂 / no=需要修改），配套 `human_blind_sample_填写说明.md` v2.0。
- 该文件交付对象是学生5（盲评），不阻塞阶段9，供阶段10 论文使用。
- ⚠️ **疑似重复文件提醒**：`新版项目/新版本论文产物/human_blind_sample_end.csv` 为 **GBK 编码**、表头也是新版 `evaluation(yes表示易懂，no表示需要修改)` 格式。它可能是一份草稿/重复件。请核对以哪份为准（建议以 `阶段7/阶段7产物/` 下 UTF-8 的 v2.0 为准），避免混淆。

### 7.【P2】gold 口径正式表述 —— 已确认

- gold 法规引用为"行业组参考代表标准"，**非实体真实违反标准**（数据可得性限制：profiles 无"违反标准"列，实体↔标准关联表在受限原始数据/角色隔离）。
- R5/R6/R7/R9 代表标准在阶段6 知识库无法规片段（`evidence_available=false`，共 21404 条引用）；评价程序须对 `evidence_available=false` **单独统计**，并在论文限制声明中说明。口径已写入 manifest。

### 8.【P2】profile_version 统一 —— 已确认

- 以 `FREEZE_20260814_001` 为准（cases/red_team/gold 均为此值）。
- 程序 loader 原写 `student1-profile-v1` 由你改为 `FREEZE_20260814_001`；补充表无 profile_version 列，以样本键对齐。

---

## 三、本文件夹产物清单（交付给学生4）

| 文件 | 说明 |
|---|---|
| `阶段9前确认回复_学生3→学生4.md` | 本回复主文档（逐条结论，含 P0 二次更正与两个疑点消解） |
| `benchmark_manifest.json` | v2.5-20260826-routeA，含 3 个 jsonl 的 SHA-256、全部阶段9前确认注记、P0 结构化决议与 2026-08-26 版本升级记录（6.0-frozen） |
| `benchmark_gold_restricted.jsonl` | 版本已对齐 6.0-frozen 的 gold 副本（SHA `bf72307a…`） |
| `profile_supplement_8fields.csv` | 真·补充表（学生1 交付，落库来源已记 manifest），P0 的 8 字段数据源 |
| `case_8fields_lookup.jsonl` | P0 已生成查表（5577 条，SHA `86165990…`），学生4 集成时直接读 |
| `p0_join_8fields_template.py` | P0 关联脚本最终执行件（已实跑生成上述查表；学生1 补齐 historical_standard_codes 后可重跑刷新） |

> cases / red_team 两文件本轮已随 2026-08-26 通知升级为 `6.0-frozen`（SHA 见 manifest），无需重发；其 canonical 位置仍在 `阶段7/阶段7产物/`。

## 四、仍需外部确认/执行的事项（非学生3 可独立闭环，须对应角色处理）

> 说明：阶段9前 8 项（P0–P2）学生3 侧已**全部闭环**。以下仅余跨角色执行项与待确认决策，不能由学生3 代决/代做。

1. **gold 受限目录迁移**（学生4 集成时执行）：将 `benchmark_gold_restricted.jsonl` 移入受限目录（如 `data/00_raw_restricted/` 或 `origin_data/from_student3/` 下受限子目录），与案例输入物理隔离；方案与声明已写入 manifest Item3 注记。
2. **historical_standard_codes 约 37% 空值补齐**（学生1）：安全锁残留，归属学生1；不阻塞阶段9，仅使该字段覆盖率约 63%。补齐后重跑 `p0_join_8fields_template.py` 刷新查表即可。
3. **blind 二元 yes/no 是否够用**（老师/学生5 确认）：当前 v2.0 已发布并交付学生5；若老师要求恢复 5 维评分，我再改回。
4. **red_team 泄漏攻击字段疑点已查证消解**：全量扫描 `red_team_cases.jsonl` 确认**无 `_leaked_label` / `_leaked_entity_proxy_id` 字段**（含 leak 字段样本数=0），无需脱敏或迁移；此前提到的"24 样本泄漏攻击"在正式文件中不存在。
