# 给学生1：画像补充表与 R1-R9 映射的补齐/确认清单

日期：2026-08-26　审核方：学生4（Stage 9 集成）
检查对象：`origin_data/from_student1/`（画像、补充表、R1-R9 映射、白名单）

## 一、总体结论

你的交付是 Stage 9 的 **Canonical Standard 唯一来源**（标准编号体系 + R1-R9 映射口径），
学生2知识库已按此补齐到 6.0-frozen（画像 144 个联邦标准全部有正文）。
你这边**数据基本不用改**，只有 1 项数据补齐建议 + 2 项书面确认。

## 二、需要做什么、为什么（按优先级）

### P1-1（建议补齐）`profile_supplement_8fields.csv` 的 `historical_standard_codes` 空值

- 现状：全量 5,337 行中约 **1,992 行（37%）为空**；Stage 9 验证集 681 条里 **235 条**为空。
- 为什么：知识库已补齐，但这些样本没有历史标准编号，Retrieval 无法构造标准查询，
  只能按"证据不足→转人工"处理（fail-close → DEFER），检索维度永远无法评价。
  这 235 条与学生2知识库无关，只能由你补数。
- 需要你做：从 `violation_clean.csv` / 历史违章记录中按 (sample_id, quarter) 回填
  `historical_standard_codes`（分号分隔的 DOL 原值，如 `19260651 J02`），并同步
  `historical_risk_categories` / `risk_category_counts` / `risk_category_unmapped_rate`，
  重跑 `final_verify.py` 后交付新 SHA-256。
- 影响：补齐后验证集"可检索样本"从 341/681 提升到 **576/681**（剩余 105 条含州法规代码，
  不在联邦知识库覆盖范围）。

### P1-2（书面确认）R1-R9 映射唯一最终版 = SHA-256 `76c0311fe830…`

- 现状：你的 `standard_to_r1r9_mapping.csv`（`76c0311fe830…`，含 R6=179）已被 Stage 9
  定为唯一权威口径（`config/experiment_config.json` 的 `data.r1r9_mapping` 已指向它）；
  学生2包内仍带旧版（`fe623738…`，无 R6），已要求学生2替换。
- 需要你做：书面确认该版本为最终冻结版（一句话即可），以便论文写明"R1-R9 以学生1 08-24
  最终版为准"。

### P2-1（说明即可）映射键 `standard_normalized` 的有损规则

- 现状：映射键规则为"4 位零填充节号 rstrip('0')"（如 `19260651→1926.0651`、
  `19260960→1926.096`），该键**有损**：`1926.106` 实际来自 `19261060`（官方 1926.1060），
  与 `19260106`（官方 1926.106）是两个不同标准。
- 为什么：Stage 9 审计/复算用 `standard_code`（无损）反推 Canonical，不受影响；
  但其他人若直接用 `standard_normalized` 当标准号会误读。
- 需要你做：在交付说明中加一句"`standard_normalized` 仅作映射查找键，标准编号以官方
  引用格式（如 `1926.651`、`1926.1060`）为准"，不必改数据。

## 三、已经确认没问题、不用改的部分

- R1-R9 映射（`76c0311f`）与学生2包内旧版仅差 27 个机械类标准的 R6/R8 归属，学生1版正确；
- 画像 `profiles_train_val.csv` + 补充表 8 字段的 Plan B 直读方案（P0 决议）继续有效；
- 白名单 v1.1（`agent_profile_whitelist.json`）与 `04_风险分类规则.md` 口径一致；
- `risk_percentile` / `model_hash` 等已确认项无需再动。

## 四、验收标准（改完后我会验证）

1. （如补齐）补充表 `historical_standard_codes` 空值率显著下降，验证集 235 → 目标 ≤ 30，
   `final_verify.py` 全量 PASS，新 SHA 登记；
2. 书面确认 `76c0311fe830…` 为唯一最终映射版本；
3. （如加说明）交付说明含映射键用途声明。

---

**一句话**：标准体系你是"源头"，数据本身没问题；最值得做的是把 37% 空的
`historical_standard_codes` 补齐（否则 235 条验证样本永远无法评价检索），
其余只是两句话的书面确认。
