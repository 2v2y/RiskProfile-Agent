# 给学生3：Benchmark/Gold 在知识库 6.0-frozen 下的补齐与修改清单

日期：2026-08-26　审核方：学生4（Stage 9 集成）
检查对象：`origin_data/from_student3/student3/学生4需要的文件/`（Stage 9 实际使用）

> 2026-08-26 更新：R1-R9 映射口径已定——**沿用学生1最终版（SHA 76c0311f，起重机归 R8）**，
> 学生2包内映射（含起重机改 R6 的 1657 版）不作为权威；你的 gold/代表标准无需因此调整。

## 一、总体结论

学生2知识库已升级为 6.0-frozen（22,781 片段 / 273 标准，覆盖画像全部 144 个联邦标准）。
你的 gold/cases/red_team/manifest 仍停留在 5.0-frozen 口径，且 gold 中 21,404 条
`evidence_available=False` 引用有 3 个代表标准现在已有片段却仍标 False。**必须同步版本并重标**，
否则检索类评价（citation_correctness 等）会系统性少算。

## 二、需要改什么、为什么（按优先级）

### P0-1 版本字段同步到 6.0-frozen

- 现状：
  - `benchmark_gold_restricted.jsonl`（学生4需要的文件版）：`knowledge_version=5.0-frozen`；
  - `from_student3` 根目录还有一套 `benchmark_cases.jsonl` / `benchmark_gold_restricted.jsonl` /
    `red_team_cases.jsonl`，`knowledge_version=4.0`（旧副本）；
  - 学生2交付说明第五节已注明"学生3需将 benchmark_cases 的 knowledge_version 改为 6.0-frozen"。
- 需要你做：
  1. 把正式使用的 `benchmark_cases.jsonl`、`red_team_cases.jsonl`、
     `benchmark_gold_restricted.jsonl` 的 `knowledge_version` 统一改为 `6.0-frozen`；
  2. 清理或明确废弃根目录 4.0 旧副本（与 5.0-frozen 版并存会造成"以哪份为准"歧义）；
  3. `benchmark_manifest.json` 的 `sources` 更新：knowledge_manifest（193 文档/4656 片段
     → **366 文档/22781 片段**）、`standard_document_mapping` 的 SHA 与版本、
     `knowledge_version` → 6.0-frozen。
- 为什么：gold/cases 的版本必须与知识库一致，否则"知识库 6.0-frozen、gold 5.0-frozen"
  的版本声明无法自洽（阶段9验收报告 P1-1 已记录过同类问题）。

### P0-2 gold 的 evidence_available 重标（3 个代表标准现在已有片段，共 10,730 条）

- 现状：gold 中 `evidence_available=False` 共 **21,404 条**引用，涉及 5 个代表标准；
  其中 3 个在 6.0-frozen 知识库已补齐片段，且这些引用的 `chunk_id` 目前是字符串 `"None"`：

  | gold 中的标准（映射键） | Canonical | 6.0 知识库是否有片段 | 引用数 | 应改为 |
  |---|---|---|---|---|
  | 1910.0036 | 1910.36 | ✅ 29 片段 | 5,337 | evidence_available=True + 补真实 chunk_id |
  | 1910.0179 | 1910.179 | ✅ 271 片段 | 5,337 | evidence_available=True + 补真实 chunk_id |
  | 1910.0146 | 1910.146 | ✅ 290 片段 | 56 | evidence_available=True + 补真实 chunk_id |
  | 1910.0001 | 1910.1 | ❌ 0 片段 | 5,337 | 维持 False（待学生2补齐） |
  | 1926.0202 | 1926.202 | ❌ 0 片段 | 5,337 | 维持 False（待学生2补齐） |

- 需要你做：对前 3 个标准，用 6.0-frozen 的真实 chunk_id 替换这些引用的 `chunk_id` 并置
  `evidence_available=True`；后 2 个维持 False，等学生2补齐后（或按论文限制声明）再处理。
- 供你使用的真实 chunk_id 对照表（3 个标准共 590 个片段）：
  `docs/交付反馈_20260826/代表标准chunk对照表_6.0frozen.csv`（gold_standard(mapping_key) / canonical /
  chunk_id / section）。每标准取若干片段引用即可，建议每个 (sample, 标准) 引用该标准第 1 个片段，
  或按你现有 29,906 条 True 引用的分配规则保持一致。
- 为什么：`citation_correctness` 只统计 `evidence_available=True` 的引用；3 个标准已有片段
  却仍标 False，等于把 **10,730 条本可评价的引用**排除在指标外，会让 R5/R6 的引用正确率
  系统性为 0，无法反映真实检索能力。

### P1-1 gold 引用与知识库版本一致性复验

- 现状：gold 中 `evidence_available=True` 的 29,906 条 chunk 引用在 6.0 新库全部存在 ✅；
  21,404 条 False 引用的 chunk_id 不在新库（旧占位，符合预期）。
- 需要你做：重标/重生成后，重新校验"True 引用 chunk_id ⊆ 新库 chunks"为 0 缺失，
  并在 manifest 记录新 gold 的 SHA-256。

### P1-1 版本字段同步到 6.0-frozen（三文件 + manifest）

- `benchmark_cases.jsonl` / `red_team_cases.jsonl` / `benchmark_gold_restricted.jsonl` 的
  `knowledge_version`：`5.0-frozen` → `6.0-frozen`；
- `benchmark_manifest.json` 的 `sources`：knowledge_manifest 更新为 **366 文档 / 22,781 片段**、
  `standard_document_mapping` 的 SHA 与版本更新为 6.0-frozen、`knowledge_version` → `6.0-frozen`；
- 清理 `from_student3` 根目录的 4.0 旧副本（cases/gold/red_team），避免"以哪份为准"歧义。

### P2-1 明确"行业组参考代表标准"口径是否需要调整

- 现状：gold 引用为行业组参考代表标准（如 R1→1910.269），非实体真实违反标准
  （manifest notes 已声明）。Stage 9 检索到的是实体历史标准（如 1910.305），两者天然不一致，
  导致 citation_correctness 偏低/失真（UNRESOLVED #5、#7）。
- 需要你做：书面确认该口径维持现状（论文写限制声明），或提供实体↔标准关联表（受限数据）作为
  替代 gold 口径。此项不阻塞实验，但影响最终结论表述。

## 三、已经确认没问题、不用改的部分

- gold 与 cases 的 (sample_id, quarter) 关联 100% 对齐（5,337 条）；
- `case_8fields_lookup.jsonl`（5,577 条）键命中正常；
- `expected_safe_defer_or_pass` 与 `stratification.evidence_sufficiency` 对应规则已确认；
- 学生1补充表 8 字段的 Plan B 直读方案（P0 决议）继续有效。

## 四、验收标准（改完后我会验证）

1. cases / red_team / gold 三文件 `knowledge_version=6.0-frozen`，manifest sources 同步；
2. gold True 引用 0 缺失，重标后的 3 个代表标准引用带真实 chunk_id；
3. 旧 4.0 副本已清理或标注废弃；
4. 新 gold SHA 已登记。

---

**一句话**：数据本身没问题，但要"版本同步 + 3 个代表标准重标 + manifest 更新"三件事，
否则 Stage 9 的检索类指标会在错误的版本和 False 标记上运行，结论不可信。
