# RiskProfile-Agent 阶段9（stage9_full 自包含版）服务器目标环境：Linux + Python 3.10/3.11

pydantic>=2.5,<3
pandas>=2.1,<3
numpy>=1.26,<2
python-dotenv>=1.0,<2

# Qwen（OpenAI 兼容）与 LangGraph 固定流程
langgraph>=1.2,<2
langchain-core>=1.6,<2
langchain-openai>=1.6,<2

# Registry / 规则文件（YAML）与 Schema 校验
pyyaml>=6.0,<7
jsonschema>=4.0,<5

# 学生2 FAISS/BGE 检索器（服务器必须安装；离线环境用预缓存 wheel，禁止联网下载）
# 缺失时 Stage9 自动回退确定性关键词检索（离线自检仍可运行）
# faiss-cpu
# sentence-transformers
# torch
