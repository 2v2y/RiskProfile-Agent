# RiskProfile-Agent 服务器目标环境：Linux + Python 3.10/3.11
# 本地开发请尽量与服务器对齐到 Python 3.11，避免 pandas/numpy 版本漂移。

pydantic>=2.5,<3
pandas>=2.1,<3
numpy>=1.26,<2
python-dotenv>=1.0,<2

# 阶段8：Qwen 通过 LangChain 的 OpenAI 兼容接口调用
# 阶段8：LangGraph 固定流程状态机
langgraph>=1.2,<2
langchain-core>=1.6,<2
langchain-openai>=1.6,<2

# 阶段8：Registry / 规则文件（YAML）与 Schema 校验
pyyaml>=6.0,<7
jsonschema>=4.0,<5
