# 阶段1 实施记录与待确认清单

记录日期：2026-08-19

## 已完成

- 按方案 31.2 搭建完整目录结构（configs / data / knowledge / benchmark / schemas / src / runs / manifests / paper / tests / docs）
- `requirements.lock`：锁定 Python 3.12.13、pydantic 2.13.4、pandas 3.0.1、numpy 2.3.5
- 四类格式规则：`profile_schema.json`、`evidence_schema.json`、`review_card_schema.json`、`audit_result_schema.json`
- 四个模块最小版本：Profile（确定性）、Retrieval（关键词）、Review（规则模板占位）、Audit（确定性核对器）
- 最小端到端流水线：画像→检索→建议→审计→建议卡，输出到不可覆盖的 `runs/` 目录，JSONL 日志 + SHA-256 清单
- 冒烟测试 `tests/smoke_test.py`：25/25 通过（含格式校验、白名单、数字核对、禁止表述、未来信息、可复现性、防覆盖）

## A. 需要队友协助（阻塞后续阶段）

### 学生1（数据）
- 【需要确认】新方案正式测试输入是否沿用旧时间切分（Test = 2024Q1—2025Q4，8 个季度）；旧 `test_features_sealed.csv` 能否直接复用，里面是否混有答案或受限字段。
- 【需要确认】旧版正式测试已于 2026-08-17 开封（`test_open_attempt.json` = success）。旧标签如何与新方案阶段11"只开一次"衔接；**旧标签一律不得进入 Agent 开发与调参**。
- 画像新增字段的原始来源核对：`historical_standard_codes` 来自 violation 表的 `standard` 字段，需要学生1在受限区确认时间可用性。

### 学生2（法规知识库）—— 阻塞 Retrieval 正式开发
- 交付五件套并给出时间：`document_inventory.csv`、`regulation_chunks.jsonl`、`knowledge_manifest.json`、`standard_document_mapping.csv`、`retrieval_gold.csv`。
- 每条 chunk 按方案 10.2 表9 的字段交付（`evidence_schema.json` 已按此定义）。
- 确认 R1—R9 分类表以哪一版为准并锁定 SHA（工作区记录表里有 `风险分类.csv`，需确认是锁定版）。

### 学生3（画像与评价集）
- 画像卡 v2 补齐字段：`historical_standard_codes`、`historical_risk_categories`、`risk_category_counts`、`risk_category_unmapped_rate`、`jurisdiction_context`、`profile_version`、`risk_score`、`risk_percentile`、`model_version`、`score_evidence`、质量标记组。
- 正式版 `agent_profile_whitelist.json`（当前 `configs/` 下是草稿，用于联调）。
- 阶段7 交付：`benchmark_cases.jsonl`、`benchmark_gold_restricted.jsonl`、`red_team_cases.jsonl`（不阻塞阶段1—2，阻塞阶段9 正式实验）。

### 学生5
- 当前不阻塞；阶段10 需要盲评协议与量表，阶段11—12 需要监督正式测试和论文证据索引。

## B. 需要学生4 / 导师拍板的决策

1. **基础模型选型**：方案要求"至少一个可本地部署的开放权重模型 + 一个性能较强的在线接口模型"，具体名称和版本在实验启动前定。开发期先用哪个默认模型跑通？
2. **"无依据陈述率"判定协议**：原子陈述如何切分（按句？）；判定由"确定性规则 + 独立 LLM + 人工抽查"如何分工；是否与 Audit 模块共用判定逻辑（方案 11.3 禁止只让另一个 LLM 自动打分）。
3. **Profile Agent 是否用 LLM**：建议 v0 用确定性规则（可复现、可审计），方案未强制；若 B3/B5 对比中用 LLM 版需在 registry 记录。
4. **Review Agent 的模型接口**：阶段8 用哪种调用方式（OpenAI 兼容 / 本地 vLLM / 其他）；API key 管理与输入匿名化合规流程。
5. **旧正式测试标签的衔接方式**（同学生1项）。
6. **运行环境**：目标机器 Python 版本与依赖锁定方式（pip-tools / conda-lock）；阶段8/9 需要的 langgraph、openai、sklearn、scipy 版本在对应阶段追加。
7. **R1—R9 分类表版本**：以当前哪份文件为锁定版本，由学生2/3共同确认。

## C. 阶段1 验收对照（方案 31.4 阶段验收1）

- 同一输入可重复产生格式正确的输出：✅ 冒烟测试（可复现性用例）
- 画像、法规证据、复核建议卡、内容审查结果四类格式规则：✅ schemas/ + Pydantic 双重校验
- 统一日志记录模型/指令/文字量/耗时/异常：✅ JSONL（Review 为模板版，指令与 token 计数在阶段8 接入 LLM 后补）
- 每个模块有正常样例和失败样例：✅ tests/fixtures + 冒烟测试
- 程序报错不覆盖已有实验输出：✅ 独立 run 目录 + FileExistsError 保护 + error 日志

---

# 大白话版（给同学直接看的版本）

我这边（学生4）已经把"机器"搭好了：画像、法规证据、建议卡、审查结果四样东西长什么样都定好了，四步流程能跑通，25 项自检全过。
但很多"原料"不在我手上。下面说人话，就是要谁给什么。

## 学生1 要给我的（管原始数据的人）
1. 正式考试的卷子：2024—2025 年 8 个季度的测试数据，**不带答案**。
   用途：最后考一次，看系统到底行不行。不给：没卷子可考。
2. 帮我确认：时间划分还用不用老方案（训练/验证/测试各是哪几年）。
   用途：保证新老实验能对上。
3. 给个说法：8 月 17 日已经"开封"过一次旧答案，这批旧答案怎么处理（作废还是继续保密）。
   用途：防止新实验有人提前看过答案，等于考试泄题。
4. 画像里新加的风险类别字段，原始出处（违章表里的标准编号）帮我核对一下什么时候能用。
   用途：确保这些数字没有用到"未来才知道"的信息。

## 学生2 要给我的（管法规的人）—— 现在最卡脖子
1. 法规库五件套（具体文件名我列在文档里了），说白了就是：
   - 官方文件有哪些（清单）
   - 法规原文切成的小段（给我的"查法规"模块当资料库）
   - 每份文件的版本和校验值（证明没乱改）
   - "标准编号 ↔ 官方条款"的对照表（查到一个标准号就能找到条款）
   - 一批"问题+正确答案"（用来考我的检索准不准）
   用途：我的查法规模块就靠这个吃饭。不给：查法规只能拿假数据顶着，B2/B5 实验全卡住。
2. 风险类别 R1—R9 分类表定稿（哪一版算数，写下来）。

## 学生3 要给我的（管画像和出题的人）
1. 画像卡升级版（v2），要加这些列：
   历史涉及的标准编号、风险类别 R1—R9、各类别出现次数、没映射上的比例、监管背景、画像版本、风险分数、季度内排名位置、分数来源、质量标记（有没有历史记录、缺不缺日期等）。
   用途：这是画像模块和建议卡的原料。现在给的画像只有 14 列，方案要求的不止这些；缺列，Agent 就少了关键信息。
2. 正式版"允许智能模块读哪些字段"的名单（我现在用的只是草稿）。
3. 以后才要（不着急现在）：考试题集（≥1000 题，带答案但答案封存）、故意刁难系统的怪题（≥200 题）。

## 学生5
现在不用管。后面做人工盲评和论文收尾。

## 需要学生4（我）或导师拍板的事
1. 用哪个大模型（本地一个 + 在线一个，各选谁）；
2. "无依据陈述率"这个主指标到底怎么算、谁来判断；
3. 画像模块用不用大模型（我建议不用，用规则，省心、可复现）；
4. 大模型接口怎么接、API key 怎么管（输入必须匿名）；
5. 运行环境怎么锁、旧答案怎么处理。

## 一句话优先级
- 现在最急：找学生2要法规库五件套，找学生3要画像卡 v2 —— 不到手，后面全卡住。
- 其次：找学生1确认测试卷子和旧答案的规矩。
- 最不急：学生3的考试题集、学生5的人工评价。
