# Report 6：学生2 第二轮验收（from_student2/交付_学生4，08-24 晚更新版）

验收时间：2026-08-24 晚
验收对象：`origin_data/from_student2/交付_学生4/交付_学生4/`（08-24 18:45 目录，含 18:13 更新的 manifest、18:12 更新的指标、17:42 更新的 chunks/mapping、18:13 更新的 rag_retriever、18:32 的交付说明）
复核方式：独立全量验证哈希、chunks/mapping 变更、gold/检索答案引用、向量库对齐、适配器解析、指标内部一致性。

## 一、验收结论

```text
学生2：PASS（核心知识库与检索交付达标）
附注：1 项 P1（包内 R1-R9 映射为旧版需替换/更正）+ 4 项 P2（见下）
```

## 二、已解决/验证通过

| 事项 | 验证结果 |
|---|---|
| 版本统一 5.0-frozen | `knowledge_manifest.json`（SHA 13ec6ebe…）与 `db_meta.json`（38f02858…）均为 5.0-frozen；推荐方法/返回条数一致 |
| retrieval_validation_metrics 补 nDCG | 12 行全部有值（0 个 N/A）；hybrid k=3：Recall=1.0000、MRR=0.8530、NDCG@10=0.8914，与 manifest/说明一致 |
| 解释资料/非法规正文编号修复 | chunks 仅 13 条 `standard` 字段变化（interpretation_* → OSHA-INTERP-*）；chunk_id 顺序与集合不变；mapping 同步 13 条；gold 29906 条 chunk 引用 0 缺失；retrieval_gold 0 缺失 |
| 向量库对齐 | chunk_ids.json（4656）与更新后 chunks 顺序完全一致，FAISS/embeddings 索引关系保持 |
| 检索策略修复 | rag_retriever.py 含 section 匹配 + float 比较；交付说明/指标/代码三者一致 |
| 交付说明 | `学生2交付说明.md` 含文件清单、修复记录、环境依赖、待学生4确认事项 |

## 三、需要处理/注意的问题

### P1：包内 `standard_to_r1r9_mapping.csv` 是旧版，与学生1最终版冲突

- 现状：包内映射（SHA `fe623738…`，6346 行）**无 R6（R6=0，R8=777）**；学生1 08-24 最终版（SHA `76c0311f…`，6346 行）**有 R6=179，R8=598**。其余类别（R1/R2/R3/R4/R5/R7/R9）行数一致。`学生2交付说明.md` 中的 R 分类表（“R1-R9（无R6）”，R3=材料储存与搬运、R5=工具与手工作业）与学生1最终版及 `04_风险分类规则.md` 官方定义不一致。
- 影响：若误用包内旧映射，机械类（R6）会被归到 R8，画像 risk_category_counts、gold R1-R9 覆盖、Retrieval 按类别检索全部错位。
- 处理：**以学生1 08-24 最终版（SHA 76c0311f…）为唯一版本**；学生2 替换包内文件并更正说明文字，或由学生4集成时只引用学生1版本并标注包内旧版作废。

### P2-1：57/4656 个片段在 stage9 适配器路径下 document_id=UNKNOWN

- 组成：OSHA-INTERP-*（13）、OSHA-FOM-Ch2（15）、OSHA-FOM-Ch3（26）、OSHA-DATA-DEFS（1）、OSHA-INSPECTION-HELP（2）。
- 原因：台账 `document_inventory.csv` 的键是 OSHA标准编号列，stage9 适配器未把“文档编号”加入查找键；stage1 适配器已处理（0 未知）。
- 影响：仅当 BGE 语义检索命中这些非法规正文片段时，证据引用会生成 `regulation:UNKNOWN#…`。标准编号精确过滤查询不受影响。
- 处理：学生4 在 stage9 适配器补 document_id 回退（参照 stage1 knowledge/adapter.py），或学生2 在台账 OSHA标准编号 列补充文档编号键。

### P2-2：向量库未随 08-24 晚 chunks 变更重建

- faiss_index.bin / embeddings.npy 仍为 08-21 13:55 生成；chunks 仅 13 条 standard 标签变化、text 未变。若嵌入只含 text 则无影响；建议学生2 说明嵌入输入是否包含 standard 字段，或确认无影响。

### P2-3：旧 zip 与新目录不一致

- `student2_1609.zip`（16:00）与 `交付_学生4.zip`（11:15）仍是旧版内容；当前目录（18:45）为最新。建议以目录为准，重打 zip 或标注旧包过期，避免误用。

### P2-4：检索验证数字与上一版差异大，建议提供评估脚本

- 08-24 11:15 版：MRR=1.0、纯BGE Recall@3=0.5054、keyword 22 条查询；08-24 晚版：MRR=0.8530、纯BGE Recall=0、keyword 93 条查询。数字变化可以解释（评估口径与检索策略改变），但建议学生2 附上评估脚本/说明以便复现。

## 四、学生4 集成待办（不再属于学生2 交付缺口）

1. 把新包（chunks/mapping/document_inventory/knowledge_manifest/retrieval_validation_metrics/rag_retriever）同步进 stage9_edit，重算 SHA 登记；
2. R1-R9 映射只使用学生1最终版（76c0311f…）；
3. stage9 适配器补 document_id 回退（57 条非法规正文片段）；
4. 离线环境安装 faiss-cpu/sentence-transformers/torch 并缓存 BGE 模型；
5. benchmark 案例使用 5.0-frozen 版（学生2/学生3 已对齐）。
