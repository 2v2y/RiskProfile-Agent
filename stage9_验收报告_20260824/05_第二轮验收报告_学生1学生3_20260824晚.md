# Report 5：第二轮验收（学生1 / 学生3 回复交付复核）

验收时间：2026-08-24 晚（学生1 `from_student1/回复学生4_20260824`、学生3 `from_student3/student3/学生4需要的文件`）
复核方式：对回复文档中的每一项声明做独立全量验证（哈希、行数、字段覆盖、关联键、映射复现、版本标签、目录存在性），不采信单点声明。

## 一、学生1：PASS（4 项全部闭环，2 个微小注记）

| 事项 | 声明 | 独立验证结果 | 判定 |
|---|---|---|---|
| model_hash 全量修复 | 1992 行截断值全部替换为 64 位 | `profile_supplement_8fields.csv` SHA `d0f1dbc9…`，5337 行 model_hash 唯一且全为 64 位，score_evidence 0 异常；备份文件 SHA `b254994a…` 与声明一致；`final_verify.py` 已改为全量检查（iloc[0] 仅作为 all_same=False 时的回退，无实际影响） | ✅ PASS |
| risk_percentile 口径 | 0—100 百分位，程序 /100 | 值域 0.4—100 与声明一致；回复文档书面确认 | ✅ PASS |
| R1—R9 映射生成源 | 提供 风险分类_R6修正.csv | SHA `3496d3c2…` 一致；8347 行、R6 最终主类 238 行、27 个唯一标准与声明一致；映射可按“standard规范值去重取最终主类”复现，R6 集合与映射完全一致（27=27） | ✅ PASS |
| 白名单唯一版本 | v1.1 为最终版 | SHA `5f2fa6a4…` 与确认包一致 | ✅ PASS |

注记（不阻塞）：
1. 映射复现时发现 `standard_to_r1r9_mapping.csv` 中有 2 个标准（`3.40`、`408.41950`）不在 R6修正 表中，且 mapping 唯一标准数 3509 vs R6修正 3507——建议学生1确认这两个异常条目是否应保留。
2. `historical_standard_codes` 仍有 1992/5337 行空值（37%）：其中 1628 行 history_inspections=0，364 行 history≥1（202 行 history=1）。学生1声明为“安全锁残留”；建议后续补齐（不阻塞阶段9，方案B 已含降级处理）。

## 二、学生3：PASS（8 项全部给出结论/产物，3 个待办注记）

| 事项 | 声明 | 独立验证结果 | 判定 |
|---|---|---|---|
| P0 案例 8 字段 | 方案B：运行时查表 | `case_8fields_lookup.jsonl`（5577 条=bench 5337+red 240），关联键 100% 命中；7 个字段 100% 有值，historical_standard_codes 覆盖 63%（空 2071/5577）；SHA `86165990…` 一致；manifest v2.4 记录 p0_resolution | ✅ PASS |
| 版本号统一 | gold 4.0→5.0-frozen | 新 gold（SHA `532e0af3…`）5337 条顶层 + 51310 处嵌套 knowledge_version 全部为 5.0-frozen；与 cases case_id 100% 对齐；manifest 声明 SHA 与本地案例 LF 归一化哈希一致（a21dece3 / d173b182） | ✅ PASS |
| 金标访问控制 | 方案已给，集成时执行 | 方案已写入 manifest；物理迁移尚未执行（学生4 集成待办） | ✅（待执行） |
| defer 规则 | 已写入 manifest | manifest 含 4175 true/1162 None 规则原文 | ✅ PASS |
| manifest 补 SHA | 已补 | output_files 含 cases/gold/red_team/lookup 四文件 SHA，与本地文件（LF 口径）一致 | ✅ PASS |
| human_blind_sample | 100×11 已交付 | 文件存在（UTF-8，100 行 11 列），分层字段齐全 | ✅ PASS（见注记） |
| gold 口径 | 已写入 manifest | “行业组参考代表标准”+ evidence_available=false 单独统计均已写入 | ✅ PASS |
| profile_version | 以 FREEZE_20260814_001 为准 | cases/red_team/gold 均为该值 | ✅ PASS（loader 修改待学生4） |

注记（不阻塞）：
1. **回复中的一处查证结论有误**：学生3回复称“red_team_cases.jsonl 无 `_leaked_label`/`_leaked_entity_proxy_id` 字段（含 leak 字段样本数=0）”。实际全量扫描 s3 与 s2 两份 red_team 均为 **48 条**记录含这些字段（24 条 gold_label_leak_attempt + 24 条 entity_proxy_id_leak_attempt）。这正是 manifest 设计的攻击样本（预期行为），但表述需要更正，避免后续文档矛盾。
2. `human_blind_sample.csv` 的 `evaluation` 列 100/100 行已预填 “yes”，建议清空后交学生5填写（或书面说明这是占位值）。
3. `case_8fields_lookup.jsonl` 无 case_id 字段；red_team 与 bench 共享 (sample_id, quarter) 键（240 个重复键），按样本键读取属方案B设计，但集成时需注意不能按案例区分。

## 三、阶段九准入判定（更新）

```text
学生1：PASS
学生2：WARNING（见下）
学生3：PASS

阶段九：
READY_WITH_WARNINGS
```

### 学生123 输入侧已达标

- 画像 + 8 字段补充表：5337 行 100% 关联、model_hash 全量修复、risk_percentile 口径确认；
- 知识库（chunks/mapping/inventory/FAISS）与检索验证指标：内容完整；
- 基准案例 5337 + red_team 240 + gold 5337（版本 5.0-frozen 全对齐、case_id 100% 对齐、chunk 引用 0 缺失）；
- 8 字段查表 5577 条（含方案B 说明）、manifest v2.4（含 SHA/规则/口径/决议记录）、盲评样本 100 条已交付。

### 剩余非阻塞事项（正式跑批前建议完成）

1. **学生4 集成**：把 `case_8fields_lookup.jsonl` 接入 dataset_loader（按 sample_id,quarter 读取）；loader profile_version 改为 `FREEZE_20260814_001`；白名单切换 v1.1；同步学生2 08-24 元数据（document_inventory/manifest/db_meta/retrieval_validation_metrics）；实现 metrics.py 接入 gold；把 gold 移入受限目录；对 historical_standard_codes 空值样本做检索降级。
2. **学生2 确认**：最终冻结标签 5.0-frozen 签字（学生3 已按此对齐）；补 nDCG 或书面说明；benchmark 案例改版归属；解释资料编号规则。
3. **学生1（可选）**：后续补齐 historical_standard_codes 1992 行空值。
4. **学生3 更正/清理**：更正 red_team 泄漏字段的查证表述；清空 blind evaluation 占位值（如确为占位）。

### 结论

学生1、学生3 的交付**已达到阶段九使用标准**，可以进入阶段九。正式实验是否“出指标”还取决于学生4 完成上述集成与指标实现（属学生4 自身工程任务，不再是学生123 交付缺口）。
